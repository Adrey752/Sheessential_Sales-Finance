@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sheessentials - Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-screen flex">

    <!-- Left Side (Blurred Background Image) -->
    <div class="w-1/2 bg-cover bg-center filter blur-sm"
         style="background-image: url('/images/login_background.jpg');">
    </div>

    <!-- Right Side (Login Panel) -->
    <div class="w-1/2 flex items-center justify-center bg-white">
        <div class="w-80 text-center space-y-5">

            <!-- Logo -->
            <img src="~/images/Logo.png" alt="Logo" class="h-80 w-100" />
            <!-- Welcome Text -->
            <h2 class="text-gray-800 text-lg font-medium">Welcome</h2>

            <!-- Login Form -->
            <form asp-controller="Auth" asp-action="Login" method="post" class="space-y-4">
                <input type="email" name="Email" placeholder="E-mail"
                       class="w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-1 focus:ring-pink-300 focus:outline-none" required />

                <div class="relative">
                    <input type="password" name="Password" placeholder="Password"
                           class="w-full border border-gray-300 rounded-md px-3 py-2 text-sm focus:ring-1 focus:ring-pink-300 focus:outline-none" required />
                    <button type="button"
                            class="absolute right-2 top-2 text-gray-400 hover:text-gray-600">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none"
                             viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                  d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                  d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                        </svg>
                    </button>
                </div>

                <div class="text-right">
                    <a href="#" class="text-xs text-blue-400 hover:underline">Forgot Your Password?</a>
                </div>

                <button type="submit"
                        class="w-full bg-emerald-900 text-white rounded-md py-2 font-medium hover:bg-emerald-800">
                    LOGIN
                </button>
            </form>
            <a href="@Url.Action("Register","Auth")" class="text-l text-blue-400 hover:underline">Register</a>
        </div>
    </div>

</body>
</html>
